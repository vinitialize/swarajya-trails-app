import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'place.run.swarajyatrails',
  appName: 'swarajya-trails',
  webDir: 'dist'
};

export default config;
