{
  "name": "swarajya-trails",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run update-version && vite build",
    "preview": "vite preview",
    "load-secrets": "node scripts/loadSecrets.js",
    "update-version": "node scripts/updateVersion.js",
    "dev:with-secrets": "npm run load-secrets && npm run dev",
    "build:with-secrets": "npm run load-secrets && npm run build"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^6.1.0",
    "@google/generative-ai": "^0.21.0",
    "@types/dompurify": "^3.0.5",
    "dompurify": "^3.2.6",
    "dotenv": "^17.2.0",
    "firebase": "^12.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "firebase-admin": "^13.4.0",
    "typescript": "~5.3.0",
    "vite": "^7.0.6"
  },
  "optionalDependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.46.0"
  }
}
